spring:
  mongodb:
    uri: mongodb://localhost:27017/eventcart_inventory_test
  data:
    mongodb:
      auto-index-creation: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: inventory-service-test

eventcart:
  security:
    enabled: false
  kafka:
    retry:
      interval-ms: 10
      max-attempts: 1
    topics:
      order-created: eventcart.orders.created
      inventory-reserved: eventcart.inventory.reserved
      inventory-failed: eventcart.inventory.failed
      payment-failed: eventcart.payments.failed
